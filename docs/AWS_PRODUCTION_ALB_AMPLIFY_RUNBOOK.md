# Moved

The **default** production path is **EC2 + nginx + Certbot + Amplify** (no ALB). Full instructions:

**[`AWS_PRODUCTION_GUIDE.md`](./AWS_PRODUCTION_GUIDE.md)**

That guide includes a short **appendix** on when you might choose **ALB + Auto Scaling** instead.

This file remains so old links do not break.
